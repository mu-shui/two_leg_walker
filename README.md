# two_leg_walker
基于强化学习的二足行走稳定控制

# 环境的搭建
在 python3.10 或者 3.11 下进行

    pip install mujoco  
    
    pip install gymnasium[mujoco]

注：
- 可以根据自己需要安装 jupyter
- 笔者使用的是 `pip install mujoco==3.4.0`
- `mujoco==3.4.0` 需要 Python 3.10 / 3.11，不支持 3.9
